# README

* Ruby version
2.4.1

Dockerでの起動方法
```
$ git clone https://github.com/tsukuru-workspace/qiita-app-api
cd qiita-app-api

# build（初回のみ）
$ docker-compose build
# rails起動
$ docker-compose up
```
